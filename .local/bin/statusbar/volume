#!/bin/sh

sound=`amixer get Master | grep -E -o '[0-9][0-9]?%' | awk '{print $1}'`
muted=`pacmd list-sinks | grep muted | awk '{print $2}'`

case $muted in
	yes) echo "Volume : MUTED";;
	no)  echo "Volume : $sound";;
esac
