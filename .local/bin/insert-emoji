#!/usr/bin/env bash

function fail () { dunstify "Failed to insert Emoji :("; }

emoji=$(cat ~/.local/share/emoji | rofi -dmenu | cut -d' ' -f1)
[[ -z "$emoji" ]] && fail;
[[ ! -z "$emoji" ]] && xdotool "$emoji";
